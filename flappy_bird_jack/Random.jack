class Random {
    static int seed;

    function void setSeed(int s) {
        let seed = s;
        return;
    }

    function int rand() {
        let seed = (seed * 31) + 57;
        if (seed < 0) { let seed = -seed; }
        return seed;
    }

    function int randRange(int min, int max) {
        var int range;
        var int r;
        let range = max - min;
        let r = Random.rand();
        return min + (r - ((r / range) * range));
    }
}